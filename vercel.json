{
  "buildCommand": "npm install --legacy-peer-deps && (npx tinacms build || echo 'TinaCMS skipped') && curl -sL https://github.com/gohugoio/hugo/releases/download/v0.158.0/hugo_extended_0.158.0_linux-amd64.tar.gz | tar xz -C /usr/local/bin && hugo --minify",
  "outputDirectory": "public",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    { "source": "/admin/(.*)", "destination": "/admin/index.html" }
  ]
}
